

#include <iostream>

int gcd(unsigned int a, unsigned int b){
	if(b == 0)
		return a;
	else
		return gcd(b % a,a);
	}

int main(int argc, char **argv)
{
	unsigned int a, b;
	cin << a;
	cin << b;
	for(unsigned)
	return 0;
}

